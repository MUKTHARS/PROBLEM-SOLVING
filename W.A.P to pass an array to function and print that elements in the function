  #include<stdio.h>
  void printarray(int* p,int n)
  {
      for(int i = 0; i<n ; i=i+1)
      {
          printf("%d ",*(p+i));
      }
  }
 
int main()

{
    int arr[] = {4,14,24,34};
    int n = sizeof(arr)/sizeof(arr[0]);
    printarray(arr,n);
   
}    

OUTPUT : 4 14 24 34
